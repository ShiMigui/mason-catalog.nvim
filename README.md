# mason-catalog.nvim
A simple Mason registry controller for Neovim. It synchronizes installed packages into an easy-to-query catalog, letting my config replace mason-conform and mason-lspconfig with a single, lightweight module.
